model: bytedance/seedance-2
prompt: "{{ ref_prompt }}"
reference_image_urls: "{{ start_image_720p }}"
reference_video_urls: "{{ reference_video }}"
generate_audio: false
resolution: "720p"
aspect_ratio: "16:9"
duration: "{{ duration }}"
web_search: false
nsfw_checker: false
